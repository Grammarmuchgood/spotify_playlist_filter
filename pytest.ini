[pytest]
# Lets "from db.database import ..."-style imports resolve without a
# manually-exported PYTHONPATH - the backend/ modules all import each
# other as if backend/ itself were the project root, since that's how
# they're actually run in production (uvicorn started from inside
# backend/).
pythonpath = backend
